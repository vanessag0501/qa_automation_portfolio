[pytest]

addopts = --tb=short
markers =
    smoke: quick critical-path tests
    regression: full regression suite
    local_only: tests that require a non-CI environment (site blocks CI runner IPs)
base_url = https://practicesoftwaretesting.com